{% extends 'base.html.twig' %}

{% block title %}Edit Nutrition Plan - Coach Dashboard{% endblock %}

{% block body %}
<div class="container py-4">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card card-custom">
                <div class="card-header" style="background-color: var(--primary-green); color: white;">
                    <h5 class="mb-0">Edit Nutrition Plan: {{ nutritionPlan.name }}</h5>
                </div>
                <div class="card-body">
                    {{ form_start(form) }}
                        <div class="mb-3">
                            {{ form_label(form.name) }}
                            {{ form_widget(form.name) }}
                            {{ form_errors(form.name) }}
                        </div>
                        
                        <div class="mb-3">
                            {{ form_label(form.description) }}
                            {{ form_widget(form.description) }}
                            {{ form_errors(form.description) }}
                        </div>
                        
                        <div class="mb-3">
                            {{ form_label(form.duration) }}
                            {{ form_widget(form.duration) }}
                            {{ form_errors(form.duration) }}
                        </div>
                        
                        <div class="mb-4">
                            {{ form_label(form.objective) }}
                            {{ form_widget(form.objective) }}
                            {{ form_errors(form.objective) }}
                        </div>
                        
                        <div class="d-flex justify-content-between">
                            <a href="{{ path('app_coach_dashboard') }}" class="btn btn-secondary">Cancel</a>
                            <button type="submit" class="btn btn-primary-custom">Update Plan</button>
                        </div>
                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}