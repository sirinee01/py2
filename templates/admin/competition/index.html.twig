{% extends 'base.html.twig' %}

{% block title %}Manage Competitions - Admin Panel{% endblock %}

{% block stylesheets %}
{{ parent() }}
<style>
    .competition-card {
        transition: transform 0.2s;
    }
    .competition-card:hover {
        transform: translateY(-5px);
    }
    .badge-upcoming { background-color: #ffc107; color: #000; }
    .badge-ongoing { background-color: #28a745; color: #fff; }
    .badge-completed { background-color: #6c757d; color: #fff; }
    .badge-cancelled { background-color: #dc3545; color: #fff; }
</style>
{% endblock %}

{% block body %}
<div class="container-fluid mt-4">
    <div class="row">
        <div class="col-12">
            <div class="d-flex justify-content-between align-items-center mb-4">
                <h1 class="h2">Manage Competitions</h1>
                <a href="{{ path('app_admin_competition_new') }}" class="btn btn-primary-custom">
                    <i class="fas fa-plus"></i> Create New Competition
                </a>
            </div>

            {% if competitions is empty %}
                <div class="alert alert-info">
                    No competitions found. <a href="{{ path('app_admin_competition_new') }}">Create your first competition</a>
                </div>
            {% else %}
                <div class="row">
                    {% for competition in competitions %}
                    <div class="col-md-4 mb-4">
                        <div class="card card-custom h-100 competition-card">
                            {% if competition.image %}
                                <img src="{{ asset('uploads/competitions/' ~ competition.image) }}" 
                                     class="card-img-top" 
                                     alt="{{ competition.name }}"
                                     style="height: 200px; object-fit: cover;">
                            {% else %}
                                <div class="bg-light d-flex align-items-center justify-content-center" 
                                     style="height: 200px;">
                                    <i class="fas fa-trophy fa-3x text-muted"></i>
                                </div>
                            {% endif %}
                            
                            <div class="card-body">
                                <h5 class="card-title">{{ competition.name }}</h5>
                                <p class="card-text text-muted small">
                                    <i class="fas fa-calendar"></i> 
                                    {{ competition.startDate|date('M d, Y H:i') }} - {{ competition.endDate|date('M d, Y H:i') }}
                                </p>
                                <p class="card-text">
                                    <i class="fas fa-map-marker-alt"></i> {{ competition.location }}
                                </p>
                                <p class="card-text">
                                    <i class="fas fa-users"></i> 
                                    Participants: {{ competition.applications|length }}/{{ competition.maxParticipants }}
                                </p>
                                <span class="badge badge-{{ competition.status }}">
                                    {{ competition.status|capitalize }}
                                </span>
                                
                                {% if competition.winner %}
                                    <div class="mt-2">
                                        <span class="badge bg-success">
                                            <i class="fas fa-crown"></i> Winner: {{ competition.winner }}
                                        </span>
                                    </div>
                                {% endif %}
                            </div>
                            <div class="card-footer bg-transparent">
                                <div class="d-flex justify-content-between">
                                    <a href="{{ path('app_admin_competition_applications', {'id': competition.id}) }}" 
                                       class="btn btn-sm btn-outline-primary">
                                        <i class="fas fa-list"></i> Applications
                                    </a>
                                    <div class="dropdown">
                                        <button class="btn btn-sm btn-outline-secondary dropdown-toggle" 
                                                type="button" 
                                                data-bs-toggle="dropdown">
                                            Actions
                                        </button>
                                        <ul class="dropdown-menu dropdown-menu-end">
                                            <li>
                                                <a class="dropdown-item" href="{{ path('app_admin_competition_edit', {'id': competition.id}) }}">
                                                    <i class="fas fa-edit"></i> Edit
                                                </a>
                                            </li>
                                            <li>
                                                <form method="post" action="{{ path('app_admin_competition_delete', {'id': competition.id}) }}" 
                                                      onsubmit="return confirm('Are you sure you want to delete this competition?');">
                                                    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ competition.id) }}">
                                                    <button type="submit" class="dropdown-item text-danger">
                                                        <i class="fas fa-trash"></i> Delete
                                                    </button>
                                                </form>
                                            </li>
                                            {% if competition.status == 'upcoming' %}
                                            <li>
                                                <a class="dropdown-item" href="#">
                                                    <i class="fas fa-play"></i> Start Competition
                                                </a>
                                            </li>
                                            {% endif %}
                                        </ul>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                    {% endfor %}
                </div>
            {% endif %}
        </div>
    </div>
</div>
{% endblock %}